Frame Geometry State Machine
============================

The frame-geometry calibration state machine is: capture a valid CCD1 header, compare firmware and host geometry fields, compute trailing dummy samples, record match status, and update dependent documentation when geometry changes.



